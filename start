#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/backend"

if [[ ! -d ".venv" ]]; then
  echo "[提示] 建议先创建虚拟环境并安装依赖: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt"
fi

exec uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
