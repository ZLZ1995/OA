name: Sync admin account tools

on:
  push:
    branches:
      - codex/admin-account-tools-action
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0
      - name: Replace branch with local source package
        shell: bash
        env:
          SYNC_ZIP_URL: https://tmpfiles.org/dl/37514089/oa-admin-account-tools.zip
        run: |
          set -euo pipefail
          curl -L "$SYNC_ZIP_URL" -o /tmp/oa-admin-account-tools.zip
          find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
          python - <<'PY'
          import zipfile
          with zipfile.ZipFile('/tmp/oa-admin-account-tools.zip') as z:
              z.extractall('.')
          PY
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Add admin password reset and account deletion"
            git push origin HEAD:codex/admin-account-tools-action
          fi
